clone repository
git clone https://github.com/apertium/WikiExtractor.git
cd WikiExtractor

run repository
python3 WikiExtractor.py --infn your_dump_file.xml.bz2 --compress


example
python3 WikiExtractor.py --infn enwiki-latest-pages-articles.xml.bz2 --output extracted --processes 4 --compress

--output OUTPUT: Specify an output directory.
--processes PROCESSES: Set the number of processes to use for parallel extraction.
--min_text_length MIN_TEXT_LENGTH: Set a minimum length for extracted documents.

apparantly hugginface offers cleaned version of wkipedeia already.
just gonna take it 

from datasets import load_dataset
dataset = load_dataset("wikipedia", "20220301.en")